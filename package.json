{
  "name": "ayush-setu-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "AYUSH Setu - Academia Industry Collaboration & Placement Portal",
  "scripts": {
    "build": "cd backend && npm install && npx prisma generate && npm run build && cd ../frontend && npm install && npm run build",
    "build:backend": "cd backend && npm install && npx prisma generate && npm run build",
    "build:frontend": "cd frontend && npm install && npm run build",
    "test": "cd backend && npx ts-node src/tests/platform.test.ts",
    "start": "cd backend && npm start",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.17.6",
    "@vercel/node": "^3.2.26",
    "prisma": "^5.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
