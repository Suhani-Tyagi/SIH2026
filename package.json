{
  "name": "ayush-setu-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "AYUSH Setu - Academia Industry Collaboration & Placement Portal",
  "scripts": {
    "build": "cd backend && npm install && npx prisma db push && npm run db:seed && npm run build && cd ../frontend && npm install && npm run build",
    "build:backend": "cd backend && npm install && npx prisma db push && npm run db:seed && npm run build",
    "build:frontend": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm start",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.2.26",
    "typescript": "^5.6.3"
  }
}
